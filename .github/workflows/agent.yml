name: Agent CI

on:
  push:
    branches: [ necro_master ]
    paths: 
      - 'src/agent/**'
  pull_request:
    branches: [ master, release-* ]
    paths: 
      - 'src/agent/**'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest]
      fail-fast: true
      max-parallel: 1
    steps:
      - uses: actions/setup-go@v2
        with:
          go-version: 1.12
      - uses: actions/checkout@v2
      - run: build_windows.bat
        shell: cmd
        working-directory: src/agent/
        if: matrix.os == 'windows-latest'
      - uses: actions/upload-artifact@v1
        with:
          name: agent
          path: src/agent/bin/